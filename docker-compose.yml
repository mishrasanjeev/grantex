services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: grantex
      POSTGRES_USER: grantex
      POSTGRES_PASSWORD: grantex
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./apps/auth-service/src/db/migrations/001_initial.sql:/docker-entrypoint-initdb.d/001_initial.sql:ro
      - ./apps/auth-service/src/db/migrations/002_spec_compliance.sql:/docker-entrypoint-initdb.d/002_spec_compliance.sql:ro
      - ./apps/auth-service/src/db/migrations/003_sandbox.sql:/docker-entrypoint-initdb.d/003_sandbox.sql:ro
      - ./apps/auth-service/src/db/migrations/004_webhooks.sql:/docker-entrypoint-initdb.d/004_webhooks.sql:ro
      - ./apps/auth-service/src/db/migrations/005_billing.sql:/docker-entrypoint-initdb.d/005_billing.sql:ro
      - ./apps/auth-service/src/db/migrations/006_policies.sql:/docker-entrypoint-initdb.d/006_policies.sql:ro
      - ./apps/auth-service/src/db/migrations/007_anomalies.sql:/docker-entrypoint-initdb.d/007_anomalies.sql:ro
      - ./apps/auth-service/src/db/migrations/008_scim_sso.sql:/docker-entrypoint-initdb.d/008_scim_sso.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U grantex"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  auth-service:
    build: ./apps/auth-service
    ports:
      - "3001:3001"
    environment:
      DATABASE_URL: postgres://grantex:grantex@postgres:5432/grantex
      REDIS_URL: redis://redis:6379
      AUTO_GENERATE_KEYS: "true"
      SEED_API_KEY: dev-api-key-local
      SEED_SANDBOX_KEY: sandbox-api-key-local
      NODE_ENV: development
      LOG_PRETTY: "false"
      PUBLIC_BASE_URL: http://localhost:3001
      JWT_ISSUER: http://localhost:3001
      ADMIN_API_KEY: local-admin-api-key
      METRICS_API_KEY: local-metrics-api-key
      VAULT_ENCRYPTION_KEY: 000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f
      COMMERCE_V1_ENABLED: "true"
      COMMERCE_ALLOW_AUTO_TENANT: "true"
      COMMERCE_LOCAL_LOAD_TEST: "true"
      PLURAL_SANDBOX_ENABLED: "true"
      PLURAL_LIVE_ENABLED: "false"
      COMMERCE_LIVE_MODE_ENABLED: "false"
      PORT: "3001"
      HOST: "0.0.0.0"
      # Stripe billing (optional — set these to enable billing endpoints)
      # STRIPE_SECRET_KEY: sk_test_...
      # STRIPE_WEBHOOK_SECRET: whsec_...
      # STRIPE_PRICE_PRO: price_...
      # STRIPE_PRICE_ENTERPRISE: price_...
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
